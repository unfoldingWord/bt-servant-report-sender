# bt-servant-report-sender configuration
# Copy this file to .env and fill in your values

# =============================================================================
# BT-SERVANT API
# =============================================================================
# Base URL of your bt-servant-engine API (no trailing slash)
BT_SERVANT_API_URL=https://your-bt-servant-api.example.com

# Admin API token for authentication
BT_SERVANT_API_TOKEN=your-admin-api-token-here

# =============================================================================
# SMTP / EMAIL
# =============================================================================
# SMTP server settings (defaults shown are for Gmail)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# SMTP authentication
# For Gmail: use your email and an App Password (not your regular password)
# Create App Password at: https://myaccount.google.com/apppasswords
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password-here

# Sender address (usually same as SMTP_USER)
EMAIL_FROM=your-email@gmail.com

# Recipients (JSON array format)
EMAIL_TO=["recipient1@example.com","recipient2@example.com"]

# =============================================================================
# OPTIONAL SETTINGS
# =============================================================================
# Default report period: daily, weekly, monthly, or custom
# REPORT_PERIOD=daily

# Output directory for PDF reports (default: ./reports)
# REPORT_OUTPUT_DIR=./reports
